#include <iostream>
using namespace std;

int main() {
	int num;
	int factorial=1;
	cout << "Input an integer: ";
	cin >> num;
	for (int i= 1; i <= num; ++i) {
		factorial *= i;
	}
	cout << "The factorial of number " << num << " is " << factorial << endl;
	return 0;
}
